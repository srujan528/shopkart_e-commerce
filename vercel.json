{
  "buildCommand": "cd server && npm install --include=dev && cd ../client && npm install --include=dev && npm run build",
  "outputDirectory": "client/build",
  "rewrites": [
    {
      "source": "/(products|brands|category|users|auth|mail|cart|orders|payment)(.*)",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
